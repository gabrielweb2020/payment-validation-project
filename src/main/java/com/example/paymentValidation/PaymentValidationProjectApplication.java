package com.example.paymentValidation;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe principal do projeto de validação de pagamentos.
 *
 * Inicia a aplicação Spring Boot e configura automaticamente os componentes e dependências necessárias.
 */
@SpringBootApplication
public class PaymentValidationProjectApplication {

	/**
	 * Método principal que inicia a aplicação Spring Boot.
	 *
	 * @param args argumentos da linha de comando (não são necessários neste contexto).
	 */
	public static void main(String[] args) {
		SpringApplication.run(PaymentValidationProjectApplication.class, args);
	}
}



